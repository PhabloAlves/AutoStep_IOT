# Override para desenvolvimento local — adiciona hot-reload ao backend.
# docker-compose.yml é a imagem de produção (sem --reload).
services:
  backend:
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
